# Implement a OpenEMS device

* for general guidance: https://openems.github.io/openems.io/openems/latest/edge/implement.html

### Communication
* the communication of a device shall use the provided Bridges (`io.openems.edge.bridge`)

## on specific device types (`natures`)
### Wallbox
* the most recent architecture have the `io.openems.edge.evse` implementations
* 
